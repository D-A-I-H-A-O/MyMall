spring:
  profiles:
    active: test
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.175.130:3306/mall_pms?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    alicloud:
      access-key: 
      secret-key: 
      oss:
        endponint: daihao.oss-cn-shenzhen.aliyuncs.com
  application:
    name: mall-product
server:
  port: 10000

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
logging:
  level:
    com.daihao.mall: debug

  jackson:
    data-format: yyyy-MM-dd HH:mm:ss

  thymeleaf:
    cache: false

